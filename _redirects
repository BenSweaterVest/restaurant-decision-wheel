# Cloudflare Pages Redirects
# Serve index.html for profile paths while preserving API routes

# API routes should not be redirected
/api/* 200

# Static assets should not be redirected
/*.js 200
/*.css 200
/*.png 200
/*.jpg 200
/*.jpeg 200
/*.gif 200
/*.svg 200
/*.ico 200
/*.json 200
/*.woff 200
/*.woff2 200
/*.ttf 200
/*.eot 200

# Redirect all other paths to index.html for client-side routing
/* /index.html 200
